# handoff 2026-07-18 — ai-meeting-assistant / #160「自動筆記手帳」MVP実装完了・最終受入5/6

## 1. HUMAN判断待ち（最初に読む）

- **判断事項1**: 既存履歴エクスポートの互換性修正を、#160 MVP完了前のフォローアップIssue/PRとして実施するか
  - 推奨: 実施する。MVP受入条件5「既存の会議フロー（エクスポート）が後退しない」を満たすため、現時点では#160を完了扱いにしない
  - 残課題A: 履歴の単一MD出力が、従来の保存済み`exportMarkdown`に含まれる要約・相談・意見・アイデア・Q&A・メモ・TODO・コストを出力しない
  - 残課題B: 新形式の単一履歴MD（`## 議事録` / `## 文字起こし`）を、既存のMDインポーターが読み戻せない
  - 判断しない場合の影響: front matter出力自体は使えるが、既存出力内容の欠落とエクスポート→インポート非互換が残る
- **判断事項2**: 本handoffのcommit・PR化
  - 推奨: 上記残課題を記録した状態で先にmergeし、次の作業起点をmainへ残す

## 2. 現在地

- 作業状態: #160のPR-1〜PR-5と、PR-5レビュー修正までmainへmerge済み
- main: `1473d8a`（PR #200 merge）
- merge済みPR: #191（保存層v3）/ #193（構造化確認UI）/ #195（履歴閲覧）/ #198（front matter MD出力）/ #199（メモ・会議プロファイル入口）/ #200（復元履歴のprofile維持）
- Issue: #159・#197はclosed、親Issue #160はopen
- MVP最終受入: **5/6 PASS、1件PENDING**。受入条件5のエクスポート回帰修正後に6/6判定する

## 3. MVP受入条件6項目の確認結果

| # | 受入条件 | 判定 | 根拠 |
|---|---|---|---|
| 1 | ワンタップ録音、停止→整理→保存が3タップ以内 | PASS | 実ブラウザで既定メモ、録音ボタン、文字起こし・状態・履歴・出力の導線を確認。P1でワンタップ入口・raw自動保存・整理導線、T1で整理→確認保存を確認。Issue #197の定義どおり「録音開始・停止・整理開始」の3タップで整理確認へ到達し、停止時点でraw保存される |
| 2 | 5件超でも過去記録を保持（v2移行含む） | PASS | HistoryStore v2→v3回帰で既存1件+新規7件=8件を保持し、v2データと既定フィールドを維持 |
| 3 | 日付・カテゴリ・タグ・全文検索で絞り込み | PASS | H1でカテゴリ・状態・期間・AND条件・文字起こし全文・構造化全文の検索、詳細表示、削除を確認。タグは検索対象として実装済み |
| 4 | front matter付きMDを1件出力し機械可読 | PASS | H2で単一MDのfront matter、YAML安全なタグ、4項目、議事録、文字起こしを確認。UnitでもYAML値のエスケープを確認 |
| 5 | 既存会議フロー（リアルタイムAI・議事録・エクスポート）が後退しない | **PENDING** | 会議プロファイルUIと既存回帰はPASS。ただしPR #198レビューで、既存履歴出力内容の欠落と新形式MDの再インポート非互換を確認。H2は新形式だけを検証するため、この回帰を検出しない |
| 6 | 録音回帰11シナリオがALL PASS | PASS | S1〜S11 ALL PASS。`recorder-lifecycle-service.js`・STT・録音コアはPR-5で無変更 |

## 4. 今回やったこと（2026-07-18）

1. **MVP 5PRを完了**: 保存層v3、停止後構造化、履歴閲覧、front matter出力、2プロファイル入口を段階的にmerge
2. **PR #199レビュー修正**: 異なる画面プロファイルで履歴を復元・上書きした際、元レコードの`profile`が変わる問題をPR #200で修正。P1へ回帰テストを追加
3. **main上で全回帰を再実行**: model registry、upload、履歴移行、T1〜T3、H1、H2、P1、S1〜S11がALL PASS
4. **実ブラウザ確認**: 既定メモでAIパネル・会議情報導線が非表示、文字起こし・状態・履歴・エクスポートが表示されることを確認。会議へ切替後はAIパネル・会議情報・会議モードが復帰し、再読み込み後も会議プロファイルが維持された
5. **MVP受入監査**: 6項目をテストとコードで照合し、受入条件5だけをPENDINGと判定

## 5. 検証結果

- `git diff --check`: PASS
- `npx eslint .`: PASS
- `npm run test:i18n`: PASS（ja/en 559 keys同期）
- `npm run test:unit`: PASS（272/272）
- `npm run test:ui-smoke`: PASS
- `npm run test:e2e`: PASS
  - Model registry: 8/8
  - Upload: Basic 6/6、Formats 7/7、Edge 9/9
  - HistoryStore v2→v3・5件超保持: PASS
  - Structuring T1〜T3: 3/3
  - History browse H1: PASS
  - History export H2: PASS
  - Recording profile P1（復元上書き含む）: PASS
  - Recorder S1〜S11: 11/11

## 6. 重要な発見・リスク

- **自動回帰が新仕様だけを保証しても、旧仕様との互換性は保証しない**: H2は新front matter契約を十分に検証する一方、従来の履歴MDに含まれた各セクションの維持と、既存インポーターでの読み戻しを検証していない
- **受入条件1の実測範囲**: デスクトップ実ブラウザで導線・表示・プロファイル永続化を確認。実マイク/APIキーを使った録音は今回行っておらず、録音処理はP1およびS1〜S11のPlaywright回帰を根拠とした
- **情報管理の注意は継続**: 録音同意、機微情報、端末紛失、外部API送信範囲、出力MDの保管は運用で管理する（設計書§6）

## 7. 次の一手（AI別）

- HUMAN: 判断事項1を決定。推奨は互換性修正Issue作成→小PRで修正→受入条件5再確認→#160クローズ
- Codex: Issue承認後、①front matterを維持しつつ従来の履歴出力内容を欠落させない、②新旧MDをインポート可能にする、③旧内容保持・round-trip回帰を追加する
- KURO: 別作業終了後に必要ならフォローアップPRの独立レビュー。今回の監督・コードレビュー・回帰確認はCodexが代行
- Gino / JEM: 追加作業なし

## 8. 参照リンク

- Issue: #160（open）/ #159・#197（closed）
- PR: #191 / #193 / #195 / #198 / #199 / #200（すべてmerge済み）
- 設計正本: `docs/design/notebook-mvp.md` §8
