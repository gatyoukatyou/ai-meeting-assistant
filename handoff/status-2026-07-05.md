# handoff 2026-07-05 — ai-meeting-assistant / 安定化リファクタリング基盤パス

## 1. HUMAN判断待ち（最初に読む）
- **判断事項**：安定化リファクタリングPR 8本のmerge可否
  - 推奨：番号順にmerge（#161 → #162 → #163 → #164 → #165 → #166 → #167 → #168）。全PRとも最新 origin/main 基点・独立ハンクのため順序依存はほぼ無いが、万一conflictが出たらKUROがrebaseする
  - 対象PR：
    - #161 test: upload-edge テスト修正（issue #147 クローズ、e2e安全網が全緑に）
    - #162 chore(lint): no-unused-vars を error 昇格（デッドコード検出ガードレール）
    - #163 fix(history): 履歴復元後にコスト表示が更新されないバグ修正
    - #164 fix(security): 内部リンクに rel=noopener 追加
    - #165 fix(security): 404.html にCSP追加＋インラインscript切り出し
    - #166 fix(stt): Deepgram 停止後のゾンビ再接続修正＋unit test 4件
    - #167 fix(stability): 履歴/IndexedDB失敗をトーストに局所化（致命的モーダル・録音停止を防ぐ）
    - #168 fix(stability): localStorage 書き込みガード＋SecureStorage 実態表記＋脅威モデル文書化
  - 期限・緊急度：急ぎなし / 中（#167 は日常利用の体感安定性に直結するため早めを推奨）
  - 判断しない場合の影響：回復可能なエラーが全画面エラー＋録音停止として現れる既存挙動が残る

## 2. 現在地
- 作業状態：安定化リファクタリング「基盤づくりパス」完了（計画どおり8本、うち1本は調査中に発見したバグ修正）
- 最新PR：#161〜#168（すべてOPEN、merge待ち）
- ctx状態：ctx/review

## 3. 今回やったこと
- 3観点（構造・安定性 / セキュリティ / 不要コード・テスト基盤）でコードベース全体を調査し、PR系列として実装
- e2e安全網を先に緑化（#161）→ lintガードレール強化（#162）→ 低リスク修正（#163-165）→ 安定性本丸（#166-168）の順で実施
- 全PRで検証済み：eslint 0 errors / unit test 全pass（#166・#168 で 217→221件に増加）/ 該当smoke・e2e / #167 はPlaywright実挙動検証7パス（トースト表示・fatalModal非表示・録音継続）

## 4. 重要な発見・リスク
- `window.onunhandledrejection` が全Promise拒否を致命的モーダル＋録音停止に直結させる設計が「日常利用での不安感」の根因（#167 で履歴系を局所化。グローバルハンドラ自体は最後の砦として温存）
- `updateCostDisplayFromState` は導入時から存在しない関数を呼ぶno-opで、履歴復元後のコスト表示が一度も更新されていなかった（#163）
- セキュリティ基礎は堅牢（CSP厳格・エスケープ一貫・キーのログ赤字化済み）。APIキーは仕様として平文保存であり、WebCrypto暗号化は同一オリジンscriptに無効なため見送り（#168 で脅威モデルをdocs/SECURITY.mdに明文化）
- 次パス候補（今回は意図的に見送り）：録音開始/停止/再起動の競合（recorder-lifecycle を js/services に抽出してから的絞り修正）、DOMContentLoaded 巨大try分割、console.* → DebugLogger 一本化、no-useless-assignment ×4 / no-unsafe-finally ×1 の解消、generateExportMarkdown 抽出（既存P1ロードマップ）

## 5. 次の一手（AI別）
- HUMAN：PR #161〜#168 のレビューとmerge判断
- KURO：merge後にCI確認、conflict発生時のrebase。次パス（録音ライフサイクル抽出）はGinoの方針確認後に着手
- Gino：次パスの優先順位（録音競合 vs export抽出）の方針判断があれば
- JEM：なし

## 6. 参照リンク
- PR: #161〜#168 ／ 関連issue: #147（#161でクローズ）
- 対応Todoistカード: プロジェクト「ai-meeting-assistant」配下の「安定化リファクタリング基盤パス」
